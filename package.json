{
  "name": "multiplayer-turnbased-web",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "overrides": {
      "@colyseus/core": "^0.16.0",
      "@colyseus/uwebsockets-transport": "npm:@colyseus/ws-transport@^0.16.5"
    }
  }
}
