apiVersion: v1
kind: Service
metadata:
  name: client
  namespace: turnbased
  labels:
    app.kubernetes.io/name: client
    app.kubernetes.io/component: frontend
spec:
  selector:
    app.kubernetes.io/name: client
  ports:
    - port: 80
      targetPort: 5173
      name: http
